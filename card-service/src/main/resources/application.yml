spring:
  application:
    name: card-service
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      uri: http://localhost:8888
      name: card-service
      profile: default
      label: main